import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Collab Todo | Collaborative Todo Lists",
  description: "Create and share collaborative todo lists instantly. No signup required.",
  keywords: ["todo", "collaboration", "task list", "productivity", "shared lists"],
  authors: [{ name: "Collab Todo" }],
  openGraph: {
    title: "Collab Todo",
    description: "Create and share collaborative todo lists instantly",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
